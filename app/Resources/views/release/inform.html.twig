{% extends 'home/show.html.twig' %}

{% block body %}

    <div class="success-message">
        {% for msg in app.session.flashBag.get('success') %}
            <div class="alert alert-success">
                {{ msg }}
            </div>
        {% endfor %}
    </div>

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Spread the Joy</h1>

                {{ include('release/_formInform.html.twig') }}


            </div>
        </div>

    </div>


{% endblock %}